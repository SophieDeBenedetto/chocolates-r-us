#!/usr/bin/env ruby

require_relative "../config/environment.rb"


# To run via the CLI:
Cli.run

# To run wihtout bells and whisltes, 
# comment out the above line, and uncomment the following line:
# ARGV.empty? ? OrderProcessor.process : OrderProcessor.process(ARGV[0])
